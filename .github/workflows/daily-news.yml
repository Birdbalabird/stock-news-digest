name: Daily Stock News Digest

on:
  schedule:
    # 01:07 UTC = 08:07 เวลาไทย (UTC+7) ทุกวัน (เลี่ยงต้นชั่วโมงเพื่อลดโอกาสโดน GitHub Actions queue ดีเลย์)
    - cron: "7 1 * * *"
  workflow_dispatch: {} # ให้กดรันเองได้จากแท็บ Actions เพื่อทดสอบ

jobs:
  send-digest:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run news digest
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
        run: python news_digest.py
